import { Project } from "@/utils/types";
import React from "react";

const BackgroundControls = ({ project }: { project: Project }) => {
  return <div>BackgroundControls</div>;
};

export default BackgroundControls;
