import { UpgradeModalProps } from "@/utils/types";
import React from "react";

const UpgradeModel = ({ isOpen, onClose, restrictedTool, reason }:UpgradeModalProps) => {
  return <div>UpgradeModel</div>;
};

export default UpgradeModel;
